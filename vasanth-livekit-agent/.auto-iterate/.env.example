# Credentials for the benchmark. Copy to .auto-iterate/.env (gitignored).
OPENROUTER_API_KEY=
# Optional: model used for both generation and judging.
MODEL=openai/gpt-4o
# Optional: use a different judge to reduce self-preference bias.
JUDGE_MODEL=openai/gpt-4o
